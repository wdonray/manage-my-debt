import type { NextPage } from 'next';
import { Layout } from '@/components';

const Home: NextPage = () => {
  return (
    <Layout>
      <section>
        Debt Repayment
      </section>
    </Layout>
  );
};

export default Home;
